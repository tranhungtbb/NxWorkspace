import { BackHistoryDirective } from './back-history.directive';

describe('BackHistoryDirective', () => {
  it('should create an instance', () => {
    const directive = new BackHistoryDirective();
    expect(directive).toBeTruthy();
  });
});
